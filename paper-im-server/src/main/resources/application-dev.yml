cors:
    filter:
        enable: true
db:
    config: profiles/dev/db.properties

logging:
    config: classpath:profiles/logback-spring.xml
server:
    tomcat:
        basedir: /tmp

javamelody:
    enabled: true
    name: javamelodyFilter
    init-parameters:
        log: true

swagger:
  enable: true


